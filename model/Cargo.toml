[package]
name = "cortex-m-spa-model"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"

[dependencies]
enum-iterator = "2.3.0"
phm = { package = "proto-hal-model", git = "https://github.com/adinack/proto-hal" }
proto-hal-build = { git = "https://github.com/adinack/proto-hal", features = ["phm"], default-features = false }

# inject local crates for tests
[patch."https://github.com/adinack/proto-hal"]
proto-hal-model = { path = "/Users/adinackerman/Documents/GitHub/proto-hal/model" }
proto-hal-build = { path = "/Users/adinackerman/Documents/GitHub/proto-hal/build" }
